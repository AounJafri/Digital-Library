<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <% if(locals.sent){ %>
        <link rel="stylesheet" href="/styles/login.css">
    <% }else{ %>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <% } %>
</head>
<% if(!locals.sent){ %>
    <body style="background-color: #50a3a2;">
        <div class="px-4 py-5 my-5 text-center">
            <!-- <img class="d-block mx-auto mb-4" src="/docs/5.3/assets/brand/bootstrap-logo.svg" alt="" width="72" height="57"> -->
            <h1 class="display-5 fw-bold text-body-emphasis">Digital Library</h1>
            <div class="col-lg-6 mx-auto">
              <p class="lead mb-4 text-body-emphasis">Welcome to Digital Library<br/>If you are a new User then Register your account otherwise go to login page by clicking Login button</p>
              <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                <button type="submit" class="btn btn-primary btn-lg px-4 gap-3" ><a href="/login" style="text-decoration: none;color: black;font-weight: bolder;">Log In</a></button>
                <button type="submit" class="btn btn-primary btn-lg px-4"><a href="/register" style="text-decoration: none;color: black;font-weight: bolder;">Register</a></button>
              </div>
            </div>
          </div>
    </body>
          <% }else{ %>
            <body>
    <div class="container">
        <% if(!locals.error) {%>
        <p>Enter Credentials to <%= sent %></p>
        <% }else{ %>
            <p><%= error %></p>
            <% } %>
        <form className="form" method="post" action="/<%= sent %>">
        <input type="text" placeholder="Username" name="username"/>
        <input type="password" placeholder="Password" name="password"/>

        <% if(locals.sent=="register"){ %>
            <input type="password" placeholder="Confirm Password"  name="confirm_password"/>
          <% } %>
        
          <button type="submit"><%= sent %></button>
      </form>
    </div>
    <% } %>
</body>
</html>